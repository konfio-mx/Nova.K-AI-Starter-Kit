pre-commit:
  parallel: true
  commands:
    lint:prettier:fix:
      glob: "*.{json,jsonc,yaml,yml,md}"
      run: task lint:prettier:fix
    lint:markdown:fix:
      glob: "*.md"
      run: task lint:markdown:fix
